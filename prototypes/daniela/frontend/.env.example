# Optional. If unset, the app calls http://127.0.0.1:8001 directly (local prototype default).
# VITE_API_BASE=http://localhost:8001
# For a deployed site where /api is on the same host, use:
# VITE_API_BASE=same
